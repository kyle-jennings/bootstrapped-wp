<?php

namespace Cascade;

/**
 * A abs (here on called Tabs).
 *
 * Tabs are  of settings for part of a section (casecadeSection like footer),
 * or something in that section (headings tags inside the footer section)
 */
class Tabs {
    public $name = '';
    public $display_name = '';
    public $fields = array();


}
